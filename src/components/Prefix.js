// @flow
import styled from 'styled-components';

const Prefix = styled.div`
    background: #ccc;
    margin-right: 16px;
    margin-left: -12px;
    margin-top: 0px;
    padding: 6px 12px;
`;

export default Prefix;
