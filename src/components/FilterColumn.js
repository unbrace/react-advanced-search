// @flow
import styled from 'styled-components';

const FilterColumn = styled.div`
  align-items: center;
  display: flex;
  float: left;
  height: 22px;
  position: relative;
`;

export default FilterColumn;
