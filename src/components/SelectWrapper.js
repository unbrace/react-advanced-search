// @flow
import styled from 'styled-components';

const SelectWrapper = styled.div`
    align-self: baseline;
    margin-top: -5px;
    position: relative;
    width: 200px;
`;

export default SelectWrapper;
